# Mobile (Flutter) — Phase 01 placeholder

This folder will contain the single Flutter app used for both Passenger and Driver.

Phase 01 tasks:
- Create Flutter project with `lib/` structure described in VS_CODE_AI_MASTER_PROMPT.md
- Implement auth and driver/passenger role flows later

Local dev (once Flutter installed):

```bash
cd himachal_transit/mobile
flutter create .
flutter run
```
